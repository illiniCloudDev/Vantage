{
  "name": "vantage-intel-root",
  "version": "1.0.0",
  "description": "Full stack financial tracker",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install && npm install --prefix backend && cd frontend && npm install && npm run build" 
  },
  "dependencies": {}
}